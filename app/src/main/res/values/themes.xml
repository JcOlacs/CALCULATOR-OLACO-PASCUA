<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />

    <style name="CalculatorButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:inset">0dp</item>
        <item name="android:backgroundTint">@color/number_color</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">26sp</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>
    
    <style name="AllClear_btn" parent="CalculatorButton">
        <item name="android:backgroundTint">@color/AC_color</item>
    </style>

    <style name="number_button" parent="CalculatorButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">@color/number_color</item>
    </style>

    <style name="operation_button" parent="CalculatorButton">
        <item name="android:backgroundTint">@color/operation_color</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>